# Deployment is handled automatically by Vercel's native GitHub integration.
# Vercel watches this repo on the main branch and deploys on every push.
# No Actions workflow needed — this file is kept as documentation only.
#
# Vercel project: saralprivacy
# Root directory: webapp/
# Production branch: main
# Dashboard: https://vercel.com/dilipsahu31s-projects/saralprivacy

name: Deploy to Vercel (handled natively)

on:
  push:
    branches:
      - main

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment note
        run: echo "Vercel deploys automatically via its native GitHub integration on push to main."
